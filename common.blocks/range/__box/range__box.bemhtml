block('range').elem('box')(
	content()(match(this._range.displayedValue)(
		function() {
			return [
				applyNext(),
				{
					elem: 'value',
					tag: 'span',
					mods: this._range.displayedValue
				}
			]
		}
	))
)